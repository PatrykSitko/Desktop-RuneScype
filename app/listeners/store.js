// import myListener from './listener/myListener';

export default (store = { dispatch, getState }) => {
  // Here you can hook event listeners into the store to dispatch actions based on state.
  //myListener(store);
};
